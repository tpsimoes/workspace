---
name: log_analyzer
description: "IT 日志分析专家技能。支持单文件或多文件联合分析，通过跨层时间线关联还原故障根因。覆盖 Linux syslog/dmesg/journal、Windows VM 事件日志（RDP / BSOD / CBS / 域加入）、Nginx/Apache、SAP HANA trace、K8s pod logs、Azure Diagnostics、网络抓包（pcap/pcapng）等主流日志格式。当用户说"分析这个日志"、"帮我看看这段报错"、"为什么崩溃"、"pcap 分析"、"抓包"、"多个日志关联"、"tcpdump"、"network trace"、"SAP trace"、"查一下日志"、"RDP 无法连接"、"BSOD"、"Windows Update 失败"、"CBS 报错"、"无法加域"、"Netlogon"、"w32tm" 时触发。"
---

# Skill: Log Analyzer
# Creator: Jack DONG

---

## 分析骨架

### Step 1 — 日志分类与分支路由

收到日志或问题描述后，先判断：
1. **日志类型 + 技术域** → 查下方路由表，匹配后加载对应 branch 参考文件
2. **是否多文件** → 若是，明确各文件的层级（OS / 中间件 / 网络）
3. **分析目标** → 用户描述的问题现象（若未提供则从日志推断）

#### 分支路由表

| 分支 | 触发信号 | 参考文件 | OS 专属 MCP |
|------|---------|---------|------------|
| **Windows VM** | BSOD · RDP 无法连接 · CBS / Windows Update 失败 · 域加入 / Netlogon / w32tm · IID/TSS/xray 包 | [references/branch-windows.md](references/branch-windows.md) | `OSBugs`, `internalkb` |
| **Linux / 集群** | OOM · kernel panic · pacemaker · fencing · syslog · dmesg · Nginx · K8s | [references/branch-linux.md](references/branch-linux.md) | — |
| **SAP** | dev_w\* · HANA trace · SM21 · short dump · HANA System Replication · NetWeaver | [references/branch-sap.md](references/branch-sap.md) | — |
| **网络抓包** | pcap · pcapng · tcpdump · network trace · 抓包 · 重传 · TLS 握手 | [references/branch-network.md](references/branch-network.md) | — |

> **多域叠加**：若日志跨多个技术域（如 Linux OS + HANA trace），同时加载多个 branch 文件。  
> **新增分支**：在此表追加一行 + 新建对应 `branch-xxx.md`，无需修改其他内容。

匹配 Windows VM 分支时，**立即跳转**到 `branch-windows.md` 执行其专属三步工作流，跳过下方通用 Step 2-5。  
其余分支继续执行下方通用流程。

---

### Step 2 — 提取关键事件

**不全量读取**，优先提取：
- `ERROR` / `WARN` / `CRITICAL` / `FATAL` 级别行
- 关键词命中行：`fail` `timeout` `refused` `killed` `OOM` `panic` `abort` `segfault` `unreachable` `reset`
- 每个命中行前后各保留 5 行上下文

pcap 文件：见 [references/branch-network.md](references/branch-network.md) 或 `scripts/pcap_analyzer.py`。  
其他格式：使用 `scripts/log_normalizer.py` 提取。

---

### Step 2.5 — 知识检索（按需触发）

遇到以下任一情况，触发 `knowledge_search` skill 补充外部知识：
- 错误码未在 branch 文件中列出
- 错误描述无法从模型知识直接解释
- 用户明确要求查找 TSG / KB 文档

`knowledge_search` 负责 ADO Wiki + Microsoft Learn 并发查询，结果用于 Step 4 根因分析。

---

### Step 3 — 时间线对齐（多文件时）

1. 统一时间戳格式（处理时区差异），参见 `scripts/log_normalizer.py`
2. 按时间顺序合并所有文件的关键事件
3. 识别跨文件的因果链，参见 [references/correlation-rules.md](references/correlation-rules.md)

---

### Step 4 — 根因分析

结合以下来源综合分析：
- Branch 文件中的错误模式 + 决策树
- Step 2.5 的知识检索结果（若已触发）
- 跨层关联规则（OS 异常 → 中间件报错 → 业务影响）
- 模型自有技术知识

---

### Step 5 — 输出报告

```
## 日志分析报告

### 基本信息
- 文件：[文件名列表]
- 时间范围：[最早] — [最晚时间戳]
- 分析目标：[用户描述或推断的问题]

### 事件时间线
| 时间 | 来源文件 | 层级 | 事件摘要 |
|---|---|---|---|

### 异常分析
#### [异常标题]
- **时间**：
- **原始日志**：`[关键行，保留原始格式]`
- **分析**：[原因解释]
- **影响**：[对上层的影响]

### 跨层关联
> [因果链描述，例如：OS OOM → 进程终止 → DB 连接断开 → 应用报 500]

### 总结
- **根因**：[一句话结论]
- **证据**：[关键证据点]
- **建议**：
  1. [立即操作]
  2. [长期改进]

### 参考资料（如触发了知识检索）
- [来源] 文档标题 — URL
```

> Windows VM 分支使用 `branch-windows.md` 中定义的 RCA 报告格式，不使用此模板。

---

## 常见陷阱

| 陷阱 | 处理方式 |
|---|---|
| 多文件时区不一致 | 先用 `log_normalizer.py` 统一为 UTC |
| SAP trace 文件编码问题 | 指定 `encoding='latin-1'` 读取 |
| pcap 文件无法直接读取 | 需要 tshark 或 pyshark，见 `branch-network.md` |
| 日志轮转导致时间跳跃 | 以文件修改时间辅助判断实际时间范围 |
| Windows evtx 二进制格式 | 先用 `python-evtx` 或 PowerShell 导出为文本再分析 |
